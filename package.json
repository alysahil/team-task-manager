{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager Full-Stack Application",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix server && npm install --prefix client && npx prisma generate --schema=./server/prisma/schema.prisma && npm run build --prefix client",
    "start": "npx prisma db push --schema=./server/prisma/schema.prisma && node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
