{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager Full-Stack Application",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm install --prefix server && npm install --prefix client && cd server && npx prisma generate && cd .. && npm run build --prefix client",
    "start": "cd server && npx prisma db push && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
